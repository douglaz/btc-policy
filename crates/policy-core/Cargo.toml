[package]
name = "policy-core"
description = "Pure PSBT policy evaluation: same inputs, same verdict on every node"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
bitcoin = { workspace = true }
miniscript = { workspace = true }

[dev-dependencies]
# V0-7 property suite only (DESIGN.md T3); never in the shipped tree.
proptest = { workspace = true }

[lints]
workspace = true
