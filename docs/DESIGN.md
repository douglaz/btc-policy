# Design: Federated Policy Vault — a t-of-n federation of policy-enforcing Bitcoin signers

Generated by /office-hours on 2026-07-13
Branch: none (repo is not yet git-initialized)
Repo: btc-policy (local)
Status: APPROVED (spend-path + duress architecture SUPERSEDED — see below)
Mode: Builder

> **⚠️ SUPERSEDED SECTIONS (2026-07-15, Model B).** [ADR-0012](adr/0012-model-b-spend-and-duress-architecture.md) is the source of truth for the spend path and duress. It reverses two decisions this doc still states as locked: nodes now **coordinate signature assembly + broadcast among themselves** (not "no intra-node communication, ever"), and the **coordinator is a pure relay, trusted only until the wrench attack** (not the trusted assembler/broadcaster). Wherever this doc has the coordinator collecting/finalizing/broadcasting, or says nodes make no outbound connections, or lists "no intra-node communication" / "coordinator trusted in MVP" as locked — ADR-0012 supersedes it. Also revised by ADR-0012: the **watchtower recognition criterion** (a node recognizes a spend it **validated**, not one it **co-signed** — the "co-signed" wording below is corrected inline), and the **duress model** (the old "answer pending then broadcast the escape and/or lockdown" is replaced by the arm → armed → fire-at-`T` → lockdown state machine). The policy/Hold/recovery/wallet-topology content below, and the watchtower's dual-role + recovery-path detection, are unaffected.

## Problem Statement

Bitcoin self-custody has no good answer to "what happens when my key is stolen" short of covenants that aren't activated. Existing options either trust a single company's off-chain policy (Swan Vault's 72-hour hold), trust a single self-hosted policy server (IDEA.md's original 2-of-2 design — one host compromise away from useless), or accept Revault-grade operational complexity. IDEA.md §5.2 identifies the core flaw in conventional 2-of-3 "policy multisig": any two user keys can bypass the policy key entirely.

This project builds a **federation of policy enforcers**: t-of-n signer nodes, each holding one multisig key *and* one policy engine, each independently refusing to sign any PSBT that violates the shared policy. The user's hardware key is mandatory alongside the federation quorum, so nodes can never spend without the user, and a stolen user key alone is refused by every honest node. Policy enforcement stops being a single point of failure and becomes majority-enforced: an attacker must compromise the user key AND t independently-hosted nodes.

On-chain shape — sketched in rust-miniscript's concrete **policy language** (the sketch below is *not* itself Miniscript; vault-cli compiles it into a Miniscript descriptor at vault creation; mainnet-deployable today):

```
or(
    and( pk(user_hw), thresh(t, node_1 ... node_n) ),
    and( older(~90 days), thresh(2, rec_a, rec_b, rec_c) )
)
```

The timelocked recovery path backstops federation loss or a lost user key.

**Terminology — "policy" does three different jobs in this project; keep them apart:**

1. **Spending policy (on-chain)**: the policy-language expression above, compiled into the vault's Miniscript descriptor and enforced by Bitcoin consensus. This is what rust-miniscript's `policy` module refers to.
2. **Policy checks (off-chain)**: the PSBT checks every vault-node runs before contributing a signature — what the `policy-core` crate implements. Unrelated to rust-miniscript's `policy` module despite the name.
3. **Policy config**: the per-node file that parameterizes the checks (allowlist, fee cap, expiry cap). See "Policy model" below.

## What Makes This Cool

- **The demo**: `btc-vault demo theft-refused` — act one: an attacker holding the user's key builds a sweep to an unknown address, and the federation refuses it with a structured, legible reason. Act two: the attacker's *allowlisted* spend is caught mid-Hold as a pending spend the user never made, and clawed back by an instant escape sweep. Later (`vault attack --scenario ...`), a whole adversarial playbook runs against a live federation on the user's own VPSes.
- **Every quorum has run the checks by construction.** Unlike 2-of-3-with-a-policy-key, there is no signature path that bypasses policy — the elegance that made the federation reframe land.
- **The deployer is the product.** `vault deploy --hosts a,b,c` makes correct topology the default instead of a security footnote — including *provider* diversity: the deployer warns when a quorum majority lands on a single hosting provider, since co-located nodes are correlated compromises. Target experience: "a few cheap VPSes and my laptop, and 15 minutes later I had a 3-of-5 vault that refused to sign a theft."
- **Empty niche.** No standalone Rust PSBT policy-evaluation crate exists (July 2026 landscape check); Keep bundles policy inside experimental FROST; Revault targets orgs. (Covenants/CTV are explicitly out of scope — this is a soft vault on today's consensus rules.)

## Constraints

- Rust throughout; mainnet-compatible under current consensus rules (no covenant dependency).
- Open source, self-hostable; audience is self-custody power users ("can I run it on my boxes?" is the acceptance test).
- **Simplicity and control over dependencies**: every dependency must beat the cost of writing it. Core deps: BDK + rust-miniscript (+ Liana's policy compiler only if it earns its keep). walletrs is reference material, not a foundation.
- Soft-vault honesty: t compromised nodes + user key = theft. The project states this openly (and demos it — the `compromised-quorum` scenario succeeds on purpose).
- Solo builder, CC-assisted; momentum matters — never more than ~a week from a runnable end-to-end loop.

## Premises

1. **Federation, not co-signer** — the vault is a user hardware key AND a t-of-n quorum (e.g., 3-of-5) of signer nodes; every node independently validates the exact PSBT against the shared policy before contributing a signature. Theft requires the user key plus t compromised nodes. Timelocked recovery backstops loss. Accepted limit: this is a soft vault, not a covenant vault.
2. **Federation runtime is the goal, crate is the foundation** — the deliverable is the node daemon (key + policy engine + coordination) plus the CLI that deploys and runs federations; the signer-agnostic PSBT policy crate is the load-bearing core each node embeds, not the end product.
3. **Lean dependency core** (revised after cross-model challenge) — BDK + rust-miniscript are the hard dependencies; Liana's descriptor/timelock pattern is the design reference for recovery; walletrs is demoted to optional reference/coordinator. Guiding rule: keep things simple and under our control; each dependency is weighed against the simplicity cost.
4. **Distribution in scope for v1** — prebuilt binaries + container images via GitHub Releases with CI (GitHub Actions); multi-VPS provisioning ships inside the CLI (SSH-based), not as separate Ansible/Terraform artifacts.

## Cross-Model Perspective

Codex (independent cold read, gpt-5.5, saw a structured summary only):

1. **Coolest version not yet considered**: a live "attack range" for Bitcoin custody — "Metasploitable for Bitcoin vaults." Not just a deployer but a reproducible adversarial lab: `attack --scenario stolen-user-key | compromised-node | compromised-quorum`, `recover --path timelock`. The scorecard shows what the vault survives (stolen key: refused; hacked VPS: refused; PSBT mutation: refused; replay: refused) and — critically — what it doesn't (compromised quorum: succeeds, explicitly showing the soft-vault trust boundary).
2. **What excites the builder most** (quoted): "I want to have a cli that is able to install the stuff on multiple vpses or similar then start the vault and show how it handles the attacker" — "They are not excited by a library. They are excited by a deployable system that proves itself under attack."
3. **50% already exists**: Liana + BDK + rust-miniscript (descriptor wallet model, timelocked recovery, PSBT lifecycle, Miniscript policy compilation). The 50% to build: federation daemon, per-node independent PSBT policy verification, TOCTOU-safe authorization, quorum coordination, anti-replay/intent binding, SSH deployer, attack harness, operational UX.
4. **Weekend build**: Rust workspace (`policy-signer` crate, `vault-node` daemon, `vault-cli`); descriptor `and(pk(user), thresh(2, node_a, node_b, node_c))`; nodes expose `POST /sign`, refuse invalid PSBTs with structured errors; CLI runs one honest spend end-to-end and one theft demo. Deliverable: `btc-vault demo theft-refused`.

Codex's implicit challenge to the original walletrs-as-foundation premise was accepted; premise 3 was revised accordingly.

## Approaches Considered

### Approach A: Minimal Federation (regtest, one command)
One Rust workspace — `policy-core` crate, `vault-node` daemon, `vault-cli`. The CLI launches 5 local nodes (3-of-5) + regtest bitcoind, funds the vault descriptor, runs one honest spend and one theft attempt that the nodes refuse. No SSH, no VPS, no watchtower yet.
Effort: S–M (CC-assisted: ~1–2 weeks of sessions). Risk: Low.
Pros: end-to-end federation demoable in days; every line survives into later phases; forces the authorization/refusal protocol design early where changes are cheap.
Cons: localhost hides ops reality (TLS, provisioning, latency, restarts); no recovery path or watchtower in v0.
Reuses: BDK (wallet/PSBT), rust-miniscript (descriptor), bitcoind regtest, Keep's signer policy fields as prior art.

### Approach B: The Attack Range (full deployer + scenario playbooks)
"Metasploitable for Bitcoin vaults": `vault deploy` provisions nodes over SSH onto real VPSes (signet), `vault attack --scenario {stolen-user-key, compromised-node, replay, mutation, stolen-recovery-keys, compromised-quorum, recovery-drill}` runs reproducible adversarial playbooks with honest pass/fail output — including the scenarios the vault loses (compromised quorum; stolen recovery keys after the timelock, unless the watchtower catches the sweep in time).
Effort: XL (CC-assisted: ~2–3 months of sessions). Risk: Medium — ops surface can swallow the novel work.
Pros: the complete power-user artifact (their boxes, real network, provable claims); the scenario scorecard becomes the project's trust currency; deployer makes correct topology the default.
Cons: months before first shareable artifact if built as one piece; provisioning code dominates the diff.
Reuses: everything from A, plus Liana's recovery pattern, container tooling.

### Approach C: Scenario-Driven Federation (attack harness as the spec)
Write the attack scenarios first as an executable acceptance suite; grow the federation to pass them one by one; publish a per-release scorecard. The harness itself is a standalone artifact other custody projects could run.
Effort: M–L (CC-assisted: ~3–5 weeks of sessions). Risk: Medium — harness abstractions designed before the system exists tend to need rework.
Pros: security claims are tests, not prose; second deliverable with independent community value; keeps development honest.
Cons: test infrastructure before the system risks abstracting wrong; slower to the first "theft refused" moment.
Reuses: A's workspace shape; proptest ecosystem; regtest harness patterns from repos/ (simple-ctv-vault's test style).

## Recommended Approach

**A now, B as a fast follow-up** (user decision). Ship the minimal co-located federation first: it gets a real theft-refusing federation running in days and keeps momentum. Approach B — the SSH deployer and attack-scenario playbooks — is Phase 2, not a someday; C's scenario scorecard folds into B as the playbook engine. A's components are real daemons co-located only by deployment choice, so B is growth, not rewrite.

Workspace shape (from Codex's weekend build, adopted; vault-proto added in eng review):

```
btc-vault/
  crates/
    policy-core/    # PSBT policy evaluation + TOCTOU-safe authorization (signer-agnostic, PURE — no wire/serde concerns)
    vault-proto/    # shared protocol vocabulary: tx commitment struct, refusal codes, /sign wire DTOs
    vault-node/     # daemon: one key, one policy engine, POST /sign, structured refusals
    vault-cli/      # coordinator + demo runner (later: SSH deployer + attack scenarios)
```

Both binaries import `vault-proto`, so a commitment-structure or refusal-code drift between coordinator and node is a compile error, not a runtime validation gap (eng review D6).

**`/sign` wire contract** (v0 shape, defined in vault-proto; two-phase per ADR-0004/0008): request `{ psbt: <base64>, escape_psbt: <base64>, pin }` — every submission carries a PIN and, for hot-class spends, the user-signed escape variant. Responses: `200 { pending: { commitment_id, first_seen, remaining_secs } }` for a hot-class spend inside its Hold (re-submission after `hold_secs` returns the signature); `200 { signed_psbt: <base64> }` for elapsed holds and instant classes (escape, refresh); `200 { refusal: { code, check, detail } }` — refusals are *outcomes*, not transport errors, and the coordinator's mixed-outcome logic must never conflate them with node-down; `400` only for input the node cannot decode. **A duress PIN returns the same `pending` response as a normal submission** — indistinguishable at the wire and on screen — while the node executes the configured duress response. Refusal-code enum: `WRONG_DESCRIPTOR`, `UNKNOWN_INPUT`, `DEST_NOT_ALLOWED`, `CHANGE_NOT_DERIVABLE`, `FEE_EXCEEDS_CAP`, `BAD_SIGHASH`, `USER_SIG_INVALID`, `COMMITMENT_EXPIRED`, `PSBT_INCONSISTENT`, `BAD_PIN`, `FRAUD_SUSPECTED` (lockdown; deliberately generic). Nodes also expose a cursor-based pull API — `GET /events?since=` — for alerts, pending spends, the sign/refuse log, and liveness (ADR-0002).

### Milestones, named once

- **v0 — the core** (the grilling pass of 2026-07-14 collapsed the former v0.1/v0.2 split): the full check set below, built on the real two-phase protocol from day one — Hold, dual PINs, duress response, watchtower duty, pull API, commitment binding, anti-replay, full test matrix. Internal build-order checkpoint, **"first light"** (not a release): an end-to-end honest spend plus one refusal on the true protocol with most checks stubbed and D=0 in config — keeps the never-more-than-a-week-from-runnable rule without shipping a throwaway one-shot `/sign`. The demo ships once, at v0 completion, with both acts: theft refused, and theft caught mid-Hold then clawed back by an escape sweep.
- **v1** — Approach B: deployer, sealing (ADR-0005), topology enforcement (ADR-0009), mTLS/Tor, per-node chain backend enforced, attack scenarios.

Every requirement in this doc is tagged with the milestone that gates it. Why the walking skeleton died: its discovery value was spent by the grill (the protocol is now designed on paper), its one-shot `/sign` contradicted the real two-phase protocol vault-proto exists to unify, and its demo undersold the product.

### Policy model

A node's policy is a **closed, hardcoded set of checks** parameterized by a per-node config file — not a rule engine, not a DSL. Adding a check type means shipping code. This is deliberate: an extensible engine is exactly the complexity the dependency-discipline constraint forbids until a real need shows up.

Per-node policy config (TOML, written once at deploy time; **policy is immutable after setup — permanently**. There is no update mechanism at any version; dynamic policy is a declared non-goal. Changing policy means provisioning a new vault and moving funds to it via the normal spend path):

```toml
policy_version = 1               # identifies the baked-at-setup policy; included in every commitment (policy never changes)
descriptor = "wsh(...)"          # the node's OWN copy of the vault descriptor
max_fee_pct = 10.0               # generous bug guard, ALL classes: fee ≤ 10% of total input value (ADR-0006)
hold_secs = 86400                # the Hold for hot-class spends; escape/refresh classes are instant (ADR-0004)
max_commitment_age_secs = 172800 # node-enforced cap on coordinator-proposed expiry (must exceed hold_secs)
max_derivation_index = 10000     # bound on own-descriptor derivation scans
pin_normal_hash = "argon2id$..." # authorizes the submitted spend
pin_duress_hash = "argon2id$..." # triggers duress_response; externally identical (ADR-0008)
duress_response = "sweep_and_lockdown"  # or "lockdown"; chosen at vault creation

[[allowlist]]                    # destination WALLETS: descriptors + bounded index, never fixed addresses
descriptor = "wpkh([hot]xpub.../<0;1>/*)"     # the hot wallet — hot class: the Hold applies
[[allowlist]]
descriptor = "wpkh([escape]xpub.../<0;1>/*)"  # the escape cold wallet — MANDATORY; escape class: instant
```

**Check set** (each check ships with pass AND fail tests — eng review D8):

| Check | Definition | Gate |
|---|---|---|
| Input ownership | Every input's prevout scriptPubKey re-derives from the node's **own** descriptor at some index ≤ `max_derivation_index`. PSBT bip32 paths are untrusted *hints* only: the node always re-derives and compares; a hint that doesn't verify is a refusal, and a missing hint triggers a bounded scan. This is also how "the expected wallet descriptor" is matched — a PSBT doesn't declare its descriptor, so ownership of every input *is* the descriptor check. | v0 |
| Destination allowlist | Every output either (a) re-derives from one of the allowlisted destination **descriptors** at some index ≤ `max_derivation_index` — comparison is scriptPubKey equality against the derived script, never string address comparison; same bounded re-derivation machinery as input ownership — or (b) is verified change. Anything else — including OP_RETURN, and an allowlisted wallet's address beyond the index bound — is refused. Descriptor entries (not fixed addresses) exist because policy is static: fixed addresses would force address reuse on the hot wallet forever. Setup requires at least two entries: hot wallet + escape cold wallet (see Wallet Topology). | v0 |
| Verified change | Output script re-derives from the node's own descriptor (either chain) at index ≤ `max_derivation_index`; the PSBT derivation path is an untrusted hint as above. An output merely *labeled* change is refused (eng review D6). | v0 |
| Fee cap | Fee (Σ inputs − Σ outputs) ≤ `max_fee_pct` × Σ inputs, all classes. A generous bug guard, not a security control: the Hold and the PIN carry the burn-attack defense, and a tight cap could refuse a panic-priced escape sweep (ADR-0006). | v0 |
| Hold | Hot-class spends are recorded as pending at first submission and signed only when re-submitted after `hold_secs`; escape and refresh classes sign instantly. The escape sweep is the implicit cancel of any pending spend (ADR-0004). | v0 |
| PIN | Every submission carries a PIN verified against the enrolled hashes: normal authorizes, duress triggers `duress_response` while answering identically (ADR-0008). No valid PIN → nothing is ever signed. | v0 |
| Sighash enforcement | The user's signature **cryptographically verifies against the node's own recomputed sighash** for the primary spend branch (presence of a `partial_sig` field is not enough), and all signatures MUST be SIGHASH_ALL (SIGHASH_DEFAULT under Taproot). This *subsumes* the old "no output mutation after authorization" check: with authorize+sign collapsed into one atomic step, any mutation after user signing invalidates the very signature the node verifies. | v0 |
| PSBT consistency | Global/input/output sections consistent and complete; undecodable input → 400 at the wire, decodable-but-inconsistent → `PSBT_INCONSISTENT` refusal. | v0 |
| Prevout ground truth | Input outpoints exist, are unspent, and match amount+script per the node's **own chain backend**. v0 trusts the PSBT's `witness_utxo` (regtest, honest coordinator); v1 must not (eng review D10). | v1 |

**Scope note**: spending delays are now IN — the Hold (ADR-0004) supersedes the earlier decision that deferred them. Velocity limits (per-day sats) remain FUTURE work; if ever added, they would be *static config* evaluated against each node's own chain view — never dynamic policy, never gossip (see locked decisions).

**Evaluation contract (the purity boundary)**: policy-core is pure — `evaluate(psbt, policy_config, resolved_prevouts, now) -> Verdict` — same inputs, same verdict on every node; federation consistency depends on it. vault-node owns all I/O: it resolves prevouts through its chain backend and supplies the clock. The backend trait lives in vault-node, never in policy-core.

**Transaction commitment** (vault-proto's core struct — inlined here so the spec stands alone; adapted from IDEA.md §5.6): `wallet_id` (hash of the vault descriptor), all input outpoints, all output `(scriptPubKey, amount)` pairs, absolute fee, `expiry` (unix seconds, coordinator-proposed; a node refuses any expiry beyond `now + max_commitment_age_secs` by its **own** clock, so a hostile coordinator cannot inflate the replay log's retention), `policy_version`. Serialization must round-trip byte-identically on coordinator and node (T4), but the encoding itself is greenfield until v1 — it may change freely between releases since both sides always ship from the same workspace.

**What the anti-replay log is — and is not** (re-derived after the authorize/sign collapse): with user-signs-first and SIGHASH_ALL, a node's signature is bound to the exact transaction, so resubmitting an identical PSBT yields the same signature on the same txid — harmless. The log does **not** defend the signature. It exists for three lesser jobs: (1) **idempotency** — an identical commitment resubmitted within its expiry returns the recorded verdict; (2) **audit** — every sign/refuse decision is on record for the incident-response loop; (3) **foundation** — velocity limits, if ever added, need exactly this record. The log is keyed by **commitment hash, never by outpoint set**: a replacement transaction (RBF fee bump, retry after failed broadcast) is a new commitment and gets a fresh policy evaluation — the log must never block legitimate replacement. v0 keeps the log in memory, pruned at expiry (eng review D9); a restarted node forgets in-flight commitments, which is acceptable because the log provides idempotency and audit, not signature security. The attack-scorecard claim "replay: refused" therefore means "an expired or tampered commitment is refused," not "the log is what stops replays." With the Hold (ADR-0004) the log gained a fourth job: it records each hot-class commitment's `first_seen` and pending status — the log IS the hold timer. Lockdown state is separate, persisted on disk, and survives restarts (ADR-0008).

**Per-node chain backend** (eng review D10): the input-ownership and prevout amount/script checks require ground truth about the chain, not the numbers a coordinator puts in the PSBT. Each node validates against **its own** Bitcoin data source (Bitcoin Core, an Electrum server, or BIP158 compact block filters) so a lying coordinator can't feed forged prevout amounts through the fee/amount checks. This also reinforces real independence — nodes share no oracle. v0 (regtest, localhost, honest coordinator) may trust the PSBT's `witness_utxo` data; v1 must not. v0 ships the backend **trait seam** with a trust-the-PSBT implementation behind it, so the lying-coordinator refusal (T6) lands in v1 without touching policy-core. Note the trait must serve double duty: prevout resolution for policy checks AND the watchtower scan (ADR-0001). Backend choice (Core vs Electrum vs filters, and the privacy/availability tradeoffs each carries) is a Phase 2 decision. Authorization is bound to the exact unsigned-transaction commitment (struct defined in the Policy model section above), never to a coordinator-supplied summary.

**Spend flow sketch** (v0; two-phase per ADR-0004/0008; each node is both policy engine and signer):

```
1. Coordinator builds the hot-class PSBT with BDK, plus the escape
   variant (same-inputs sweep to the escape wallet).
2. User signs BOTH on their key, every time — the normalized
   two-transaction ceremony — then enters a PIN at submission.
   (Software key in the regtest demo; hardware via cyberkrill later.)
3. Coordinator POSTs {psbt, escape_psbt, pin} to each node's /sign.
4. Each node independently: verifies the PIN, decodes both PSBTs,
   recomputes the tx commitment, evaluates policy-core checks,
   cryptographically verifies the user signatures against its own
   recomputed sighashes (a present-but-unverified partial_sig is a
   refusal), records the commitment as pending with first_seen —
   and answers `pending`. Recovery-branch PSBTs are refused
   outright: nodes hold no recovery keys and never co-sign that
   path. No node trusts any summary.
5. Normal PIN + re-submission after the Hold → node signs the hot
   tx. Duress PIN → [SUPERSEDED by ADR-0012 — the node ARMS the
   user-signed escape and holds it silently until T, then broadcasts
   + locks down (arm → armed → fire-at-T → lockdown); it does NOT
   broadcast on submission. Original text:] node answers `pending`
   identically, then executes duress_response (broadcast the escape
   variant and/or enter lockdown). Escape- and refresh-class spends
   skip the Hold and sign at step 4.
6. [SUPERSEDED by ADR-0012 — nodes assemble the t partials over the node channel and broadcast; the coordinator does NOT collect/finalize/broadcast. Original text follows for history:] Coordinator collects t node signatures, finalizes, broadcasts —
   and between submissions polls GET /events on every node for
   pending spends, alerts, sign-log reconciliation, and liveness.
```

User-first signing means a node's signature never exists on a transaction the user hasn't committed to, and each node's policy pass is bound to its own signature — there is no separate authorization artifact to steal or replay; the Hold inserts a visible waiting period, not a stealable token.

Decisions locked in eng review:

- **Mixed outcome (quorum reached, some node refused): proceed and broadcast, but every refusal emits a mandatory structured alert** naming the refusing node and reason. One wedged or hostile node can never freeze funds (that's what t-of-n buys), but a refusal is always surfaced as an incident — it means config drift or a partially caught attack, never noise. The compromised-node attack scenario demos exactly this. v0 alert artifact, concretely: a structured JSON event on the coordinator's stdout plus a scorecard field and distinct exit path in the demo output — greppable and testable (T5). A durable user-facing channel is Next Steps step 3 work.
- **Anti-replay log is bounded**: entries are pruned once their commitment expires (every commitment carries an expiry, capped by the node's own `max_commitment_age_secs`), so node state stays small; v0 keeps it in memory. What the log does and does not defend is spelled out in the Policy model section.
- **Key generation is on-node**: each vault-node derives its private key on its own host at provision time; only the public key travels back to the CLI, which assembles the descriptor from pubkeys. No machine ever holds two node keys — the t-of-n independence claim is true from key birth. MVP key handling: keys live **in memory only**, wskdf-derived at startup from an operator-held preimage (see locked decisions below) — nothing at rest on the node. Future option: encrypted key import, where a key is uploaded to a node encrypted against that node's ephemeral public key (restore/migration without plaintext transit).
- **Node API access control**: v0 nodes bind 127.0.0.1 only. v1 uses mTLS — the deployer mints per-node certificates at provision time and `/sign` requires a client cert — so policy probing and flooding die at the transport layer. v1 nodes are also exposed as Tor hidden services (NAT traversal + endpoint privacy; Tor client authorization can layer on top of mTLS).

Decisions locked in the open-questions pass (2026-07-13, user):

- **Script type: P2WSH now, Taproot later.** v0 and the first mainnet-capable release use `wsh()` (boring, best device support today); Taproot is a future migration, not a v1 blocker. With a fixed descriptor template the descriptor is hand-written — Liana's policy compiler is not needed, since spending policies never become user-configurable (next bullet).
- **Policy is static, permanently.** Once vault setup completes there is NO mechanism to change policy — not in v1, not later; dynamic policy is a declared non-goal for any reasonable future. This deletes what was the sharpest open edge (authenticating policy *changes*) by construction: an attacker holding the user key cannot edit the allowlist, because nobody can. Changing policy means provisioning a new vault and moving funds to it through the normal spend path (the rotate flow).
- **3-of-5 is the default federation** (demo and recommendation): it shows the federation properties properly — two nodes can be down or refusing and the vault still works, while theft needs three compromised hosts plus the user key. t and n remain config parameters.
- **No intra-node communication, ever.** Nodes never talk to each other; a node's only inputs are `/sign` requests and its own view of the blockchain. Any future stateful check (e.g., velocity) would be computed per-node from its own chain view — never from gossip. This collapses the former coordination-transport and shared-policy-state questions.
- **Coordinator is trusted in the MVP.** The malicious-coordinator rules in this doc (D6 change re-derivation, D10 chain backend) define the *v1 hardening target*, not MVP requirements. The threat-model enumeration (T9) becomes v1 work.
- **Node keys are in-memory for the MVP, wskdf-derived.** No signing-key material at rest: each node derives its key at startup via wskdf (https://github.com/douglaz/wskdf — Argon2id, memory-hard, from a small operator-held preimage). A restart requires re-derivation (~30s per pass, by design). TPM/HSM hardening stays a post-MVP option.
- **Wire format is greenfield until v1.** vault-proto's encodings (including the commitment serialization) may change freely between releases; coordinator and nodes always ship from the same workspace, so no cross-version compatibility is promised before v1.
- **CTV/covenants are out of scope.** No covenant backend seam, no covenant-readiness requirement: this is a soft vault on today's consensus rules, full stop.

Decisions locked in the grilling pass (2026-07-14, user; full rationale in repo `docs/adr/0001`–`0009`, glossary in repo `CONTEXT.md`):

- **Nodes are the watchtower** (ADR-0001): each node watches its own chain view for recovery-path spends and vault spends it never co-signed.
- **Coordinator pulls alerts/logs/liveness** (ADR-0002): nodes make no outbound connections; suppression/latency risk accepted. The coordinator also reconciles pulled sign-logs against its own history — a sign event it didn't originate means someone else is driving the vault.
- **Key-independence matrix** (ADR-0003): escape ⊥ user ⊥ recovery keys; CLI enforces best-effort (fingerprint tripwire), the setup ceremony carries the rest.
- **The Hold** (ADR-0004): hot-class spends wait `hold_secs` (default 24h) as visible pending spends before nodes sign; escape/refresh classes are instant; the escape sweep is the implicit cancel.
- **Sealed nodes** (ADR-0005): post-setup, no SSH, no admin path, no upgrade-in-place; provider console + 2FA is the documented residual perimeter.
- **Fee cap is a bug guard** (ADR-0006): one `max_fee_pct = 10` for all classes; the Hold and PIN carry the burn defense.
- **Reboot = node death** (ADR-0007): accepted for MVP; 3-of-5 is the budget; coordinator polling doubles as liveness monitoring; `/unseal` designed but deferred.
- **Duress PINs** (ADR-0008): a PIN on every spend (doubling as a second factor — a stolen user key alone can no longer get anything signed); two-transaction signing ceremony; `duress_response ∈ {lockdown, sweep_and_lockdown}` (default `sweep_and_lockdown`) chosen at creation; fraud-flavored refusals as cover story; lockdown has no reset on sealed nodes — the exit is the recovery path.
- **No correlation class reaches quorum** (ADR-0009): deployer-enforced; wizard preaches 5 sealed VPSes across ≥3 providers; the coordinator laptop must not itself become a correlation class (no saved provider-console sessions).
- **Recovery timelock**: per-vault descriptor parameter, 180-day preached default; refresh nags at 120/150 days.
- **v0.1 walking skeleton killed**: one v0 core milestone on the real protocol, with an internal "first light" checkpoint; the demo ships once, with both acts.

## Wallet Topology

The complete system is four wallets/keysets plus four mechanisms (locked 2026-07-13, user):

| # | Component | Kind | Role |
|---|---|---|---|
| 1 | **Vault** | wallet, two spend paths | Primary storage. Path A (normal): user key + 3-of-5 federation, policy-checked. Path B (recovery): `older(~90d)` + 2-of-3 recovery keys — used for **loss only**, never as an attack response. |
| 2 | **Hot wallet** | allowlisted destination | Daily spending; the risk budget. Static policy means it can never be un-allowlisted: if its keys are compromised, stop sending to it and rotate the vault when convenient. |
| 3 | **Recovery keyset** | 2-of-3 cold keys | Not a funded wallet — an alternate spend path over the same coins. Distribute geographically/socially (e.g., owner, bank box, lawyer/heir): this doubles as the **inheritance mechanism**. |
| 4 | **Escape cold wallet** | allowlisted destination, **mandatory at setup** | Single-sig offline hardware, keys independent of everything else. The rotate/race destination. Exists *because* policy is static: the allowlist baked at setup is every place the vault can ever pay, so the incident destination must be in it from day one. |

Mechanisms:

- **Watchtower** (v0; ADR-0001/0002, **recognition criterion revised by [ADR-0012](adr/0012-model-b-spend-and-duress-architecture.md)**): every node, against its own chain view — alerts on (a) any recovery-path spend and (b) any vault UTXO spend it never **validated** (saw and policy-checked the request) — NOT "never co-signed", which false-alarms on the n−t nodes that legitimately don't sign each spend. The coordinator pulls alerts and additionally reconciles node sign-logs against its own spend history to catch spends it didn't initiate.
- **Coin refresh**: a self-spend resets a coin's relative timelock. It runs through the normal path, so it **requires the user's hardware key** — the CLI batches and prepares refresh PSBTs; the user signs on a ~90-day cadence. Full automation is impossible by construction; the cadence is a UX parameter, not an implementation detail.
- **Rotate flow**: normal-path sweep of everything to the escape wallet after a compromise signal (stolen user key, compromised node, recovery-key race). Funding a successor vault happens from the escape wallet at leisure. Pre-generating a successor-vault descriptor and allowlisting it directly is a possible v2 refinement; the MVP keeps the escape wallet as the single incident destination.
- **Descriptor backup**: the full vault descriptor (all pubkeys). Without it, even valid recovery keys cannot find or spend the coins. Public-ish data — back it up promiscuously (paper, every node, the coordinator, alongside each recovery key). Also powers watch-only wallets and the watchtower.

**Recovery triggers (exhaustive)** — unrefreshed UTXOs *arm* the recovery branch; only loss *triggers* it:

1. Lost user hardware key → stop refreshing, wait out the timelock, recovery-sweep.
2. Lost federation (3+ nodes permanently gone) → same.
3. Inheritance/incapacity → heirs assemble 2 of the 3 recovery keys and wait.
4. Post-duress lockdown (ADR-0008) → the federation has no reset on sealed nodes; once safe, funds re-emerge exclusively through the recovery path.

An *unauthorized* recovery-path spend (stolen recovery keys) is answered by the **normal** path — watchtower alert, then race the remaining coins to the escape wallet — never by recovery itself.

**Scenario → response matrix:**

| Scenario | Detected by | Response | Path |
|---|---|---|---|
| Stolen user key | nothing signs without a PIN; sweep attempts → refusal alerts; allowlisted attempts hang in the Hold as pending spends the user never made | escape sweep (cancels any pending spend), then rotate | normal |
| Stolen user key **+ hot-wallet keys** (composite) | the pending spend, during the Hold | escape sweep before the Hold elapses; if missed, loss bounded by the hot balance (the declared risk budget) | normal |
| Coercion / wrench attack | the user signals silently via the duress PIN — externally identical to compliance | `duress_response`: escape sweep and/or lockdown, presented as automated fraud prevention; funds re-emerge via the recovery path once safe | normal → recovery |
| ≤2 nodes compromised/wedged | mixed-outcome refusal alerts | rotate (a node key is baked into the descriptor, so replacing a node = new vault) | normal |
| User key + 3 nodes compromised | watchtower (theft succeeds) | loss — the accepted soft-vault boundary | — |
| Stolen recovery keys (≥2) | node watchtowers see the recovery-path spend | best-effort race of remaining coins to escape; the real defense is refresh discipline — refreshed coins were never exposed | normal |
| Lost user key | user | wait out timelock, recovery sweep | recovery |
| Lost federation (3+ dead — incl. accumulated reboot deaths, ADR-0007) | coordinator liveness polling | wait out timelock, recovery sweep (rotate earlier, at 1–2 dead) | recovery |
| Death/incapacity | heirs | same | recovery |
| Hot wallet compromised | user/wallet | stop paying it; rotate when convenient (allowlist is immutable) | normal |
| Coordinator lost (data only) | user | rebuild from descriptor backup; no funds at risk | — |
| Coordinator **auth-key** lost (ADR-0012) | user | normal path BRICKED — the pubkey is pinned in the immutable manifest, so every future request (spend, refresh, escape) is rejected; only exit is the recovery timelock. Needs a backup/rotation story. | recovery |

## Open Questions

- **Refresh batching and privacy**: the recovery hazard itself is now fully designed (relative timelock counts from each UTXO's confirmation; 180-day default; node watchtowers alert on recovery-path spends; refresh = instant-class self-spend the user signs on nag). What remains open is the refresh *strategy*: consolidate UTXOs per refresh (cheaper fees, worse privacy and larger single-coin exposure) versus refresh-in-place per coin (costlier, preserves the UTXO set). Decide at Next Steps step 2 with real fee data.
- **PIN transport hardening**: nodes receive PINs in plaintext for hash-comparison, so a compromised node learns them (ADR-0008 accepts this for MVP — a node minority can't use them). Revisit alongside v1 mTLS: per-node PIN salting or PAKE-style verification if it earns its complexity.
- **`/unseal` adoption timing** (ADR-0007): reboot-death is accepted for MVP; the sealed-empty + preimage-reinjection design is on file. Adopt when real federations show reboot attrition faster than natural rotation cadence.
- **Hardware-key integration via cyberkrill**: integrate the user key through cyberkrill (https://github.com/douglaz/cyberkrill — Rust; PSBT signing, multisig descriptor support; devices: Coldcard, Trezor, Jade, Tapsigner/Satscard). Open sub-question: which of those devices can register/display the vault's full Miniscript descriptor versus blind-signing the PSBT — verify per device once the descriptor freezes. cyberkrill-core is also a candidate reference (or dependency, subject to the simplicity rule) for the node chain-backend abstraction — it already speaks Bitcoin Core RPC, Electrum, and Esplora.
- **Untrusted-coordinator hardening (v1)**: when the trusted-coordinator MVP assumption is lifted, write the coordinator threat-model enumeration (T9 — what a compromised coordinator can and cannot do: leak privacy, deny service, forge PSBTs, attempt replay, suppress alerts; D11's coordinator-side alerting is knowingly vulnerable to that last one, accepted) and enforce the D10 chain backend. Until then these are documented v1 targets, not MVP work.

## Success Criteria

- **v0 (the core)**: `btc-vault demo theft-refused` works from a clean clone with one command on a machine with Docker (or local bitcoind): 5 nodes launch (3-of-5), vault funds on regtest, honest spend confirms (through the Hold, time-warped on regtest), then **act one** — a theft attempt refused by all nodes with a structured reason — and **act two** — an allowlisted theft caught mid-Hold as a pending spend the user never made, clawed back by an instant escape sweep. Exit code communicates the scorecard. Full Policy model check set live; entire test matrix green (both branches per check, node API adversarial tests, quorum/mixed-outcome, anti-replay/Hold, duress, lockdown persistence).
- **v1 (Approach B)**: a power user with a few cheap VPSes and a laptop goes from `cargo install` (or binary download) to a running signet federation in under 15 minutes, and every published attack scenario runs against it reproducibly — including the honest failure (`compromised-quorum: succeeds`).
- policy-core has property tests (proptest) over PSBT mutation: no mutated transaction passes an authorization bound to the original.

## Distribution Plan

- GitHub repository with the Rust workspace; `cargo install btc-vault` plus prebuilt binaries (Linux x86_64/aarch64, macOS) attached to GitHub Releases.
- Container image for `vault-node` published to GHCR (the SSH deployer pulls it, or installs the bare binary).
- CI: GitHub Actions — test + clippy on PR; release workflow builds binaries and images on tag.
- The demo path must not require VPSes: regtest demo runs fully local.

## Next Steps

1. **v0 core (the assignment)**: scaffold the 4-crate workspace on the real two-phase protocol from day one; hardcode a 3-of-5 + user-key P2WSH descriptor — **no recovery branch yet** (the regtest demo vault is throwaway); t, n, `hold_secs`, and PIN config are parameters from day one. **First light** (internal checkpoint, ~week one, not a release): end-to-end honest spend + one refusal from 5 local vault-node processes on the true `{psbt, escape_psbt, pin}` protocol, most checks stubbed, D=0 in config. Then build out: the full check set from the Policy model (verified change, fee-pct guard, sighash enforcement, PSBT consistency, chain-backend trait seam), the Hold with pending state, dual PINs + duress response + lockdown persistence, watchtower duty + `GET /events` pull API, commitment binding, anti-replay with expiry pruning, proptest suite over PSBT mutations, and the full test matrix — every policy check on both branches; node API tests (malformed PSBT → 400 no panic, missing/invalid user sig or PIN → refusal, wrong-descriptor PSBT → refusal); coordinator tests (2 down on 3-of-5 → succeeds, 3 down → clean insufficient-quorum, mixed outcome → broadcast + alert); Hold tests (pending → still pending before D → signed after D; escape sweep cancels pending); duress tests (identical wire response, response executes, lockdown survives restart); anti-replay tests. Demo ships at the end of this step with both acts. Test plan artifact: `~/.gstack/projects/btc-policy/user-unknown-eng-review-test-plan-20260713.md`.
2. **Honest-path completeness (before-real-sats gate)**: recovery descriptor path (Liana pattern; per-vault timelock, 180-day preached default) with coin-refresh nags (from day 120, shrill from day 150; CLI prepares batches, the user signs), the setup ceremony in the CLI (dual-PIN enrollment with distinctness check, key-independence tripwires per ADR-0003, escape-wallet requirement, descriptor backup), coordinator persistence, and the incident-response loop: pulled alerts surface to the user, and the CLI offers the rotate flow (escape sweep via the normal path; fund the successor vault from there — see Wallet Topology). Watchtower duty already ships in v0 (ADR-0001) — recovery without monitoring was the blocker, and it's gone.
2.5. **"Core proven" gate (eng review D12)** — a hard milestone between the v0 core and any ops build-out. The deployer, sealing, Tor, mTLS, and recovery-automation work does NOT start until: the full test matrix (step 1) is green, a real spend has confirmed on signet through the federation (Hold included), and the policy-core/node has had at least one honest external review. This exists so the actual product (the refusal logic) can't be starved by ops plumbing under schedule pressure. Codex and this review independently reached the same instinct: the valuable core is "a policy-enforcing multisig signer with rigorous PSBT refusal tests," and the v1 surface is large enough to drown it.
3. **Approach B begins** (only after the core-proven gate): SSH provisioning then **sealing** (`vault deploy --hosts` provisions, configures, uninstalls SSH — ADR-0005), correlation-rule enforcement (no provider/location/account hosts 3 nodes — ADR-0009), mTLS/Tor between coordinator and nodes, signet support, `attack --scenario` playbook engine with the nine scenarios (stolen-user-key, compromised-node, mutation, replay, stolen-recovery-keys, compromised-quorum, recovery-drill, **hold-clawback**, **duress-drill**), scorecard output, and the untrusted-coordinator hardening (threat-model enumeration T9, enforced per-node chain backend). Policy remains immutable-at-setup at every version — dynamic policy is a non-goal, so no policy-update mechanism is ever built.
4. **Ship it**: README with the regtest-demo quickstart (local, no VPS needed; the 15-minute figure is the VPS deploy target), GitHub Release with binaries, demo GIF of both acts (refusal + hold-clawback), post to the self-custody community.

## Implementation Tasks
Synthesized from this review's findings. Each task derives from a specific finding above. Run with Claude Code or Codex; checkbox as you ship. (P1 blocks ship; P2 lands same phase; P3 is a follow-up.)

- [ ] **T1 (P1, human: ~1 day / CC: ~30min)** — provisioning — On-node key generation; only pubkeys travel to the CLI
  - Surfaced by: Architecture D4 — no machine may ever hold two node private keys
  - Files: `crates/vault-node/` (keygen), `crates/vault-cli/` (pubkey collection + descriptor assembly)
  - Verify: integration test asserts CLI never receives a node private key; descriptor built from collected pubkeys
- [ ] **T2 (P1, human: ~2 days / CC: ~1 session)** — policy-core — Full policy check set with pass+fail unit tests
  - Surfaced by: Test D8 — every check needs both branches; attacker-labeled change output must be refused
  - Files: `crates/policy-core/`
  - Verify: `cargo test` covers allowlist, fee boundary (at/over/under), input ownership, sighash enforcement (subsumes output mutation), consistency, change-derivation-from-own-descriptor
- [ ] **T3 (P1, human: ~1 day / CC: ~30min)** — policy-core — proptest suite over PSBT mutation + anti-replay with expiry pruning
  - Surfaced by: Test D8 + Performance D9 — no mutated tx passes original authorization; log bounded by expiry
  - Files: `crates/policy-core/`
  - Verify: proptest green; replay of identical/expired commitment refused; pruning test asserts bounded state
- [ ] **T4 (P1, human: ~1 day / CC: ~30min)** — vault-proto — Shared commitment/refusal/DTO crate
  - Surfaced by: Code Quality D6 — coordinator/node type drift is a security-boundary bug
  - Files: `crates/vault-proto/` (new), imported by node + cli
  - Verify: single definition; drift is a compile error; commitment round-trips identically on both sides
- [ ] **T5 (P1, human: ~1 day / CC: ~30min)** — coordinator — Mixed-outcome handling: broadcast at quorum + structured refusal alert
  - Surfaced by: Code Quality D7 — one refusing node must not freeze funds, but refusals must surface
  - Files: `crates/vault-cli/`
  - Verify: test (3-of-5) — 3 sign, 1 refuses, 1 down → tx broadcasts AND alert emitted with the refuser's reason; 3 down → clean insufficient-quorum
- [ ] **T6 (P1 for v1; v0 ships only the backend trait seam with a trust-the-PSBT impl — human: ~2 days / CC: ~1 session)** — vault-node — Per-node chain backend for prevout/input validation (trait serves policy checks AND the watchtower scan)
  - Surfaced by: Outside voice D10 — fee/amount/input-ownership checks must not trust coordinator-supplied PSBT data
  - Files: `crates/vault-node/` (backend trait + impls: trust-PSBT (v0), then Core/Electrum/BIP158)
  - Verify: v0 — trait seam exists, demo passes through it; v1 — lying coordinator (forged prevout amount) → node refuses after backend cross-check
- [ ] **T7 (P1, human: ~half day / CC: ~15min)** — process — "Core proven" gate before ops build-out
  - Surfaced by: Outside voice D12 — ops plumbing must not starve the refusal-logic core
  - Files: n/a (milestone in README/roadmap)
  - Verify: gate checklist — full test matrix green + confirmed signet spend + one external review — recorded before deployer work starts
- [ ] **T8 (P2, human: ~2 days / CC: ~45min)** — deployer — mTLS on /sign (v1); localhost-only bind (v0)
  - Surfaced by: Architecture D5 — node endpoints need client auth, not just TLS
  - Files: `crates/vault-node/` (bind + client-cert enforcement), `crates/vault-cli/` (cert minting at deploy)
  - Verify: v0 node rejects non-loopback; v1 node rejects a client without a valid cert; Tor hidden-service exposure layered on top
- [ ] **T9 (P3, human: ~1 day / CC: ~30min)** — docs — Coordinator threat-model enumeration
  - Surfaced by: TODO D13 — scattered malicious-coordinator assumptions need one auditable contract
  - Files: design doc / future SECURITY.md (capture as a bead)
  - Verify: section enumerates coordinator can/cannot do (privacy, DoS, policy drift, PSBT forgery, replay, alert suppression)
- [ ] **T10 (P1, v0)** — policy-core/vault-node — The Hold: two-phase `/sign`, pending state in the commitment log, per-class instant/held routing (ADR-0004)
  - Verify: hot-class → `pending`; re-submit before D → still pending; after D → signed; escape/refresh classes sign instantly; escape sweep double-spends a pending spend's inputs
- [ ] **T11 (P1, v0)** — vault-node/vault-cli — Dual PINs, duress response, lockdown persistence (ADR-0008)
  - Verify: no/wrong PIN → `BAD_PIN`, nothing signed; duress PIN → wire response byte-identical to normal `pending` + configured response executes; lockdown survives node restart; post-lockdown everything → `FRAUD_SUSPECTED`
- [ ] **T12 (P1, v0)** — vault-node — Watchtower duty + `GET /events` pull API (ADR-0001/0002)
  - Verify: recovery-path spend on regtest → alert queued; vault spend the node never co-signed → alert; cursor semantics (no loss, no duplication); coordinator log-reconciliation flags a sign event it didn't originate
- [ ] **T13 (P2, v1)** — deployer — Sealing + correlation-rule enforcement (ADR-0005/0009)
  - Verify: post-deploy host has no SSH; lockdown state survives provider-console reboot; 3 nodes on one provider/account/location → hard warning

## What I noticed about how you think

- You didn't accept the design doc you yourself brought in. When I locked "2-of-2 user + policy server" into a premise, you pushed back: "we want a sort of 'federation' of signers and policy enforcers, each one with a key. so the attacker must get the majority of keys." Challenging your own three-week-old written design mid-session is rarer than challenging someone else's.
- You said "the library, it's the foundation but not the goal. the goal is the federation that will run and keep the wallet robust" — and yet when asked for the fastest path you chose "policy-signer crate first." Holding both of those at once (systems goal, foundations discipline) is the combination that makes security software actually ship.
- When Codex challenged the walletrs premise you didn't just flip to its answer — you added your own rule: "dependencies need to be balanced with the simplicity tradeoff." You metabolize outside opinions into principles instead of swapping one authority for another.
- "A cli that is able to install the stuff on multiple vpses... then start the vault and show how it handles the attacker" — your instinct for proof-by-demonstration over proof-by-whitepaper is exactly what your chosen audience (power users) responds to.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 1 | issues_found | outside voice raised chain-backend, alert-channel, over-scope; 3 tensions resolved |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | clean | 9 issues, 0 critical gaps, all folded into design |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | — |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

- **CODEX:** outside voice found 4 gaps the four sections missed; 3 accepted (per-node chain backend D10, core-proven gate D12, change-derivation-from-own-descriptor D6-refinement), 1 rejected (independent alert channel D11 — user kept coordinator-side alerting).
- **CROSS-MODEL:** Codex and this review independently reached the same over-scope instinct (valuable core = policy-enforcing signer + rigorous refusal tests); resolved by inserting the core-proven gate rather than cutting features.
- **VERDICT:** ENG CLEARED — ready to implement. Scope reduced (design hardened, no scope cut). Chain backend, on-node keygen, vault-proto, mixed-outcome broadcast+alert, and the core-proven gate are locked into the design.
- **OPEN-QUESTIONS PASS (2026-07-13, user):** all previously open concerns resolved in one pass — P2WSH now/Taproot later; **policy static permanently** (dynamic policy a declared non-goal; deletes the policy-provenance question by construction); 3-of-5 default federation; no intra-node communication ever (nodes see only /sign + their own chain view); coordinator trusted in MVP (untrusted-coordinator rules become v1 targets); MVP node keys in-memory via wskdf; wire format greenfield until v1; hardware keys via cyberkrill (Coldcard/Trezor/Jade/Tapsigner/Satscard); CTV/covenants out of scope. Open Questions section now holds only: recovery-monitoring/coin-refresh details, per-device descriptor support via cyberkrill, and v1 untrusted-coordinator hardening.
- **GRILLING PASS (2026-07-14, user; /grill-with-docs):** relentless one-decision-at-a-time interview; 9 ADRs written to repo `docs/adr/` and a 29-term glossary to repo `CONTEXT.md`. Major outcomes: nodes are the watchtower; coordinator pulls alerts (suppression risk accepted); key-independence matrix with best-effort enforcement; **the Hold** (off-chain unvault period — supersedes delays-out-of-scope; escape sweep as implicit cancel); **duress PINs** with lockdown/sweep responses and fraud-flavored cover story; **sealed nodes** (no SSH post-setup; lockdown exit = recovery path); reboot = node death (MVP); fee cap demoted to 10% bug guard; no correlation class reaches quorum; recovery timelock 180d default; v0.1 walking skeleton killed in favor of one v0 core milestone with an internal first-light checkpoint. Composite attack (user key + hot keys) and coordinator log-reconciliation added to the threat model.
- **WALLET-TOPOLOGY PASS (2026-07-13, user):** added the Wallet Topology section — four wallets/keysets (vault, hot, recovery keyset, escape cold wallet) + four mechanisms (watchtower, coin refresh, rotate flow, descriptor backup), exhaustive recovery-trigger list (loss + inheritance only; attacks are answered via the normal path), and the scenario→response matrix. Two decisions locked: the **escape cold wallet is a mandatory setup-time allowlist entry** (static policy makes the allowlist the vault's permanent set of possible destinations, so the incident destination must be baked in), and **allowlist entries are descriptors with a bounded index, not fixed addresses** (avoids permanent address reuse; reuses the input-ownership re-derivation machinery). Coin refresh is documented as inherently semi-manual (requires the user hardware key on a ~90-day cadence).
- **FRESH-EYES CLARITY AUDIT (2026-07-13, post-approval):** a zero-context cold read surfaced 20 clarity findings. Folded in: terminology firewall for "policy" (spending policy vs policy-core checks vs config), policy-language-vs-Miniscript label fix, Policy model section (config schema, check-set table with per-check definitions and milestone gates, explicit velocity/delay out-of-scope statement), evaluation purity contract, inlined commitment struct with node-capped expiry, anti-replay re-derivation (log = idempotency/audit, keyed by commitment hash, never blocks RBF), v0.1/v0.2 milestone rename (retires the contradictory "v0"), T6 retag (trait seam v0.2, enforcement v1), `/sign` wire contract + refusal-code enum, v0.1 descriptor scope (no recovery branch; t/n configurable), alert artifact definition. Defaults set and open to veto: fee cap is absolute sats; script type working default `wsh()` (now an explicit Open Question); commitment canonical serialization fixed at T4.

NO UNRESOLVED DECISIONS
